import UIKit

func capitalizeFirstLetter(_ string: String) {
    
    let firstSpace = string.firstIndex(of: " ")!
    let firstWord = string[..<firstSpace]

    
    for char in string {
        if char == " " {
            
        }
    }
    
}

capitalizeFirstLetter("Alex is the one")
